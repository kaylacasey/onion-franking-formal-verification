theory trying
begin
builtins: asymmetric-encryption

rule Send:
let
mrt = <~x, ~y, ~z>
in
    [Fr(~k),
    Fr(~x), Fr(~y), Fr(~z)]
    -->
    [Out(aenc(mrt, ~k)), !Key($A, $B, ~k)]

rule Rec:
    let mrt = adec(aenc(mrt, ~k)) in
    [!Key($A, $B, ~k), In(aenc(mrt, ~k))]
    -->
    []

end